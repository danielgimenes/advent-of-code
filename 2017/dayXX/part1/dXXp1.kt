package dayXX.part1

import utils.*

fun main() {
    var line = readStr()
    // var numbers = readInts()
    while (line != null) {
        println(doSomething(line))
        // println(doSomethingInts(numbers))
        line = readStr()
        // numbers = readInts()
    }
}

// fun doSomethingInts(numbers: String): Int {
fun doSomething(line: String): Int {
    return 1
}
